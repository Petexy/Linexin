# Override /usr/lib/modules-load.d/nvidia-utils.conf from the nvidia-utils package
# which force-loads nvidia-uvm at boot. We handle nvidia/nouveau switching via
# kernel command line parameters in the boot entries instead.
