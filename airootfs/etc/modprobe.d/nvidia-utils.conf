# Override /usr/lib/modprobe.d/nvidia-utils.conf from the nvidia-utils package
# which blacklists nouveau. We handle nouveau/nvidia switching via kernel
# command line parameters in the boot entries instead.
